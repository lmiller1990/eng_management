<% content_for :title, "New action item" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl mb-5">New action item for Meeting #<%= @meeting.id %></h1>

  <%= render "form", action_item: @action_item, meeting: @meeting %>

  <%= link_to "Back to meeting", @meeting, class: "mt-4 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
