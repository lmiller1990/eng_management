<% content_for :title, "Editing action item" %>

<div class="max-w-2xl">
  <h1 class="text-4xl font-bold mb-6">Editing action item for Meeting #<%= @meeting.id %></h1>

  <%= render "form", action_item: @action_item, meeting: @meeting %>

  <%= link_to "Back to meeting", @meeting, class: "btn btn-ghost mt-4" %>
</div>
