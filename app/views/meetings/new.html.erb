<% content_for :title, "New meeting" %>

<div class="max-w-2xl">
  <h1 class="text-4xl font-bold mb-6">New meeting</h1>

  <%= render "form", meeting: @meeting %>

  <%= link_to "Back to meetings", meetings_path, class: "btn btn-ghost mt-4" %>
</div>
