<div id="<%= dom_id meeting %>" class="flex-1">
  <h2 class="card-title"><%= meeting.title.present? ? meeting.title : "Meeting ##{meeting.id}" %></h2>
  <p class="text-sm opacity-60">Created <%= meeting.created_at.strftime("%B %d, %Y") %></p>
</div>
