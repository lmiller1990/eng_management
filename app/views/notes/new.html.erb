<% content_for :title, "New note" %>

<div class="max-w-2xl">
  <h1 class="text-4xl font-bold mb-6">New note for Meeting #<%= @meeting.id %></h1>

  <%= render "form", note: @note, meeting: @meeting %>

  <%= link_to "Back to meeting", @meeting, class: "btn btn-ghost mt-4" %>
</div>
