#!/bin/bash -e

echo "Starting rails app!"

echo "Entrypoint argv: $@" >&2

if [ "$RAILS_ENV" = "test" ]; then
  echo "Preparing test database…" >&2
  ./bin/rails db:prepare
  ./bin/rails db:fixtures:load
fi

exec "$@"